"use client";

import { Spinner } from "@/components/Spinner";

// Layout handles the redirect. This page just shows a spinner during navigation.
export default function CalendarRoot() {
  return <Spinner />;
}
